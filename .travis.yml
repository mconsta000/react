sudo: required
services:
    - docker
    
before_install:
    - docker build -t mconsta000/react -f Dockerfile.dev . 
    
script:
    - docker run -e CI=true mconsta000/react npm run test -- --coverage
    

